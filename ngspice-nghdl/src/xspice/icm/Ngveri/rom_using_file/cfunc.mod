/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

                
        #include <stdio.h>
        #include <math.h>
        #include <string.h>
        #include "sim_main_rom_using_file.h"

        
void cm_rom_using_file(ARGS) 
{
	Digital_State_t *_op_data, *_op_data_old;

    static int inst_count=0;
    int count=0;
        
    if(INIT)
    {   
        inst_count++;
        PARAM(instance_id)=inst_count;
        foorom_using_file(0,inst_count);
        /* Allocate storage for output ports and set the load for input ports */

        
        port_address=PORT_SIZE(address);

        port_read_en=PORT_SIZE(read_en);

        port_ce=PORT_SIZE(ce);

        port_data=PORT_SIZE(data);
		cm_event_alloc(0,8*sizeof(Digital_State_t));
		/* set the load for input ports. */
		int Ii;
		for(Ii=0;Ii<PORT_SIZE(address);Ii++)
		{
			LOAD(address[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(read_en);Ii++)
		{
			LOAD(read_en[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(ce);Ii++)
		{
			LOAD(ce[Ii])=PARAM(input_load); 
		}

		/*Retrieve Storage for output*/
		_op_data = _op_data_old = (Digital_State_t *) cm_event_get_ptr(0,0);


	}
	else
	{
		_op_data = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_data_old = (Digital_State_t *) cm_event_get_ptr(0,1);
	}

	//Formating data for sending it to client
	int Ii;
	count=(int)PARAM(instance_id);

    for(Ii=0;Ii<PORT_SIZE(address);Ii++)
    {
        if( INPUT_STATE(address[Ii])==ZERO )
        {
            temp_address[Ii]=0;            }
        else
        {
            temp_address[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(read_en);Ii++)
    {
        if( INPUT_STATE(read_en[Ii])==ZERO )
        {
            temp_read_en[Ii]=0;            }
        else
        {
            temp_read_en[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(ce);Ii++)
    {
        if( INPUT_STATE(ce[Ii])==ZERO )
        {
            temp_ce[Ii]=0;            }
        else
        {
            temp_ce[Ii]=1;
        }
            }
	foorom_using_file(1,count);

	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(data);Ii++)
    {
        if(temp_data[Ii]==0)
        {
            _op_data[Ii]=ZERO;
            }
        else if(temp_data[Ii]==1)
        {
            _op_data[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(data[Ii]) = _op_data[Ii];
            }
        else if(_op_data[Ii] != _op_data_old[Ii])
        {
            OUTPUT_STATE(data[Ii]) = _op_data[Ii];
            OUTPUT_DELAY(data[Ii]) = ((_op_data[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(data[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(data[Ii]) = STRONG;
    }

}