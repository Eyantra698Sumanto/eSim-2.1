/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

                
        #include <stdio.h>
        #include <math.h>
        #include <string.h>
        #include "sim_main_user_encoded.h"

        
void cm_user_encoded(ARGS) 
{
	Digital_State_t *_op_data_out, *_op_data_out_old;

    static int inst_count=0;
    int count=0;
        
    if(INIT)
    {   
        inst_count++;
        PARAM(instance_id)=inst_count;
        foouser_encoded(0,inst_count);
        /* Allocate storage for output ports and set the load for input ports */

        
        port_clk=PORT_SIZE(clk);

        port_data_in=PORT_SIZE(data_in);

        port_reset=PORT_SIZE(reset);

        port_data_out=PORT_SIZE(data_out);
		cm_event_alloc(0,1*sizeof(Digital_State_t));
		/* set the load for input ports. */
		int Ii;
		for(Ii=0;Ii<PORT_SIZE(clk);Ii++)
		{
			LOAD(clk[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(data_in);Ii++)
		{
			LOAD(data_in[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(reset);Ii++)
		{
			LOAD(reset[Ii])=PARAM(input_load); 
		}

		/*Retrieve Storage for output*/
		_op_data_out = _op_data_out_old = (Digital_State_t *) cm_event_get_ptr(0,0);


	}
	else
	{
		_op_data_out = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_data_out_old = (Digital_State_t *) cm_event_get_ptr(0,1);
	}

	//Formating data for sending it to client
	int Ii;
	count=(int)PARAM(instance_id);

    for(Ii=0;Ii<PORT_SIZE(clk);Ii++)
    {
        if( INPUT_STATE(clk[Ii])==ZERO )
        {
            temp_clk[Ii]=0;            }
        else
        {
            temp_clk[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(data_in);Ii++)
    {
        if( INPUT_STATE(data_in[Ii])==ZERO )
        {
            temp_data_in[Ii]=0;            }
        else
        {
            temp_data_in[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(reset);Ii++)
    {
        if( INPUT_STATE(reset[Ii])==ZERO )
        {
            temp_reset[Ii]=0;            }
        else
        {
            temp_reset[Ii]=1;
        }
            }
	foouser_encoded(1,count);

	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(data_out);Ii++)
    {
        if(temp_data_out[Ii]==0)
        {
            _op_data_out[Ii]=ZERO;
            }
        else if(temp_data_out[Ii]==1)
        {
            _op_data_out[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(data_out[Ii]) = _op_data_out[Ii];
            }
        else if(_op_data_out[Ii] != _op_data_out_old[Ii])
        {
            OUTPUT_STATE(data_out[Ii]) = _op_data_out[Ii];
            OUTPUT_DELAY(data_out[Ii]) = ((_op_data_out[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(data_out[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(data_out[Ii]) = STRONG;
    }

}