/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

        
        #include <memory>
        #include <verilated.h>
        #include "Vram.h"
        #include <stdio.h>
        #include <stdio.h>
        #include <fstream>
        #include <stdlib.h>
        #include <string>
        #include <iostream>
        #include <cstring>
        using namespace std;
        
        extern "C" int temp_clk[1024];
        extern "C" int port_clk;
        extern "C" int temp_reset[1024];
        extern "C" int port_reset;
        extern "C" int temp_cs[1024];
        extern "C" int port_cs;
        extern "C" int temp_read[1024];
        extern "C" int port_read;
        extern "C" int temp_write[1024];
        extern "C" int port_write;
        extern "C" int temp_datawrite[1024];
        extern "C" int port_datawrite;
        extern "C" int temp_address[1024];
        extern "C" int port_address;
        extern "C" int temp_dataread[1024];
        extern "C" int port_dataread;
        extern "C" int fooram(int,int);
        
        void int2arrram(int  num, int array[], int n)
        {   
            for (int i = 0; i < n && num>=0; i++) 
            {
                array[n-i-1] = num % 2;
                num /= 2;
                }
        }
        int arr2intram(int array[],int n)
        {   
            int i,k=0;
            for (i = 0; i < n; i++) 
                k = 2 * k + array[i];
            return k;
        }
        
        int fooram(int init,int count) 
        {
            static VerilatedContext* contextp = new VerilatedContext;
            static Vram* ram[1024];
            count--;
            if (init==0) 
            {
                ram[count]=new Vram{contextp};
                contextp->traceEverOn(true);
            }
            else
            {
                contextp->timeInc(1);
                printf("=============ram : New Iteration===========");
                printf("\nInstance : %d\n",count);
                printf("\nInside foo before eval.....\n");
				printf("clk=%d\n", ram[count] ->clk);
				printf("reset=%d\n", ram[count] ->reset);
				printf("cs=%d\n", ram[count] ->cs);
				printf("read=%d\n", ram[count] ->read);
				printf("write=%d\n", ram[count] ->write);
				printf("datawrite=%d\n", ram[count] ->datawrite);
				printf("address=%d\n", ram[count] ->address);
				printf("dataread=%d\n", ram[count] ->dataread);
				ram[count]->clk = arr2intram(temp_clk, port_clk);
				ram[count]->reset = arr2intram(temp_reset, port_reset);
				ram[count]->cs = arr2intram(temp_cs, port_cs);
				ram[count]->read = arr2intram(temp_read, port_read);
				ram[count]->write = arr2intram(temp_write, port_write);
				ram[count]->datawrite = arr2intram(temp_datawrite, port_datawrite);
				ram[count]->address = arr2intram(temp_address, port_address);
				ram[count]->eval();

                printf("\nInside foo after eval.....\n");
				printf("clk=%d\n", ram[count] ->clk);
				printf("reset=%d\n", ram[count] ->reset);
				printf("cs=%d\n", ram[count] ->cs);
				printf("read=%d\n", ram[count] ->read);
				printf("write=%d\n", ram[count] ->write);
				printf("datawrite=%d\n", ram[count] ->datawrite);
				printf("address=%d\n", ram[count] ->address);
				printf("dataread=%d\n", ram[count] ->dataread);
				int2arrram(ram[count] -> dataread, temp_dataread, port_dataread);

            }
            return 0;
        }