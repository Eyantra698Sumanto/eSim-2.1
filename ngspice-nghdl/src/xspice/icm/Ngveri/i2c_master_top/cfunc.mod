/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

                
        #include <stdio.h>
        #include <math.h>
        #include <string.h>
        #include "sim_main_i2c_master_top.h"

        
void cm_i2c_master_top(ARGS) 
{
	Digital_State_t *_op_wb_dat_o, *_op_wb_dat_o_old;
	Digital_State_t *_op_wb_ack_o, *_op_wb_ack_o_old;
	Digital_State_t *_op_wb_inta_o, *_op_wb_inta_o_old;
	Digital_State_t *_op_scl_pad_o, *_op_scl_pad_o_old;
	Digital_State_t *_op_scl_padoen_o, *_op_scl_padoen_o_old;
	Digital_State_t *_op_sda_pad_o, *_op_sda_pad_o_old;
	Digital_State_t *_op_sda_padoen_o, *_op_sda_padoen_o_old;

    static int inst_count=0;
    int count=0;
        
    if(INIT)
    {   
        inst_count++;
        PARAM(instance_id)=inst_count;
        fooi2c_master_top(0,inst_count);
        /* Allocate storage for output ports and set the load for input ports */

        
        port_wb_clk_i=PORT_SIZE(wb_clk_i);

        port_wb_rst_i=PORT_SIZE(wb_rst_i);

        port_arst_i=PORT_SIZE(arst_i);

        port_wb_adr_i=PORT_SIZE(wb_adr_i);

        port_wb_dat_i=PORT_SIZE(wb_dat_i);

        port_wb_we_i=PORT_SIZE(wb_we_i);

        port_wb_stb_i=PORT_SIZE(wb_stb_i);

        port_wb_cyc_i=PORT_SIZE(wb_cyc_i);

        port_scl_pad_i=PORT_SIZE(scl_pad_i);

        port_sda_pad_i=PORT_SIZE(sda_pad_i);

        port_wb_dat_o=PORT_SIZE(wb_dat_o);

        port_wb_ack_o=PORT_SIZE(wb_ack_o);

        port_wb_inta_o=PORT_SIZE(wb_inta_o);

        port_scl_pad_o=PORT_SIZE(scl_pad_o);

        port_scl_padoen_o=PORT_SIZE(scl_padoen_o);

        port_sda_pad_o=PORT_SIZE(sda_pad_o);

        port_sda_padoen_o=PORT_SIZE(sda_padoen_o);
		cm_event_alloc(0,8*sizeof(Digital_State_t));
		cm_event_alloc(1,1*sizeof(Digital_State_t));
		cm_event_alloc(2,1*sizeof(Digital_State_t));
		cm_event_alloc(3,1*sizeof(Digital_State_t));
		cm_event_alloc(4,1*sizeof(Digital_State_t));
		cm_event_alloc(5,1*sizeof(Digital_State_t));
		cm_event_alloc(6,1*sizeof(Digital_State_t));
		/* set the load for input ports. */
		int Ii;
		for(Ii=0;Ii<PORT_SIZE(wb_clk_i);Ii++)
		{
			LOAD(wb_clk_i[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(wb_rst_i);Ii++)
		{
			LOAD(wb_rst_i[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(arst_i);Ii++)
		{
			LOAD(arst_i[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(wb_adr_i);Ii++)
		{
			LOAD(wb_adr_i[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(wb_dat_i);Ii++)
		{
			LOAD(wb_dat_i[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(wb_we_i);Ii++)
		{
			LOAD(wb_we_i[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(wb_stb_i);Ii++)
		{
			LOAD(wb_stb_i[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(wb_cyc_i);Ii++)
		{
			LOAD(wb_cyc_i[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(scl_pad_i);Ii++)
		{
			LOAD(scl_pad_i[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(sda_pad_i);Ii++)
		{
			LOAD(sda_pad_i[Ii])=PARAM(input_load); 
		}

		/*Retrieve Storage for output*/
		_op_wb_dat_o = _op_wb_dat_o_old = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_wb_ack_o = _op_wb_ack_o_old = (Digital_State_t *) cm_event_get_ptr(1,0);
		_op_wb_inta_o = _op_wb_inta_o_old = (Digital_State_t *) cm_event_get_ptr(2,0);
		_op_scl_pad_o = _op_scl_pad_o_old = (Digital_State_t *) cm_event_get_ptr(3,0);
		_op_scl_padoen_o = _op_scl_padoen_o_old = (Digital_State_t *) cm_event_get_ptr(4,0);
		_op_sda_pad_o = _op_sda_pad_o_old = (Digital_State_t *) cm_event_get_ptr(5,0);
		_op_sda_padoen_o = _op_sda_padoen_o_old = (Digital_State_t *) cm_event_get_ptr(6,0);


	}
	else
	{
		_op_wb_dat_o = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_wb_dat_o_old = (Digital_State_t *) cm_event_get_ptr(0,1);
		_op_wb_ack_o = (Digital_State_t *) cm_event_get_ptr(1,1);
		_op_wb_ack_o_old = (Digital_State_t *) cm_event_get_ptr(1,2);
		_op_wb_inta_o = (Digital_State_t *) cm_event_get_ptr(2,2);
		_op_wb_inta_o_old = (Digital_State_t *) cm_event_get_ptr(2,3);
		_op_scl_pad_o = (Digital_State_t *) cm_event_get_ptr(3,3);
		_op_scl_pad_o_old = (Digital_State_t *) cm_event_get_ptr(3,4);
		_op_scl_padoen_o = (Digital_State_t *) cm_event_get_ptr(4,4);
		_op_scl_padoen_o_old = (Digital_State_t *) cm_event_get_ptr(4,5);
		_op_sda_pad_o = (Digital_State_t *) cm_event_get_ptr(5,5);
		_op_sda_pad_o_old = (Digital_State_t *) cm_event_get_ptr(5,6);
		_op_sda_padoen_o = (Digital_State_t *) cm_event_get_ptr(6,6);
		_op_sda_padoen_o_old = (Digital_State_t *) cm_event_get_ptr(6,7);
	}

	//Formating data for sending it to client
	int Ii;
	count=(int)PARAM(instance_id);

    for(Ii=0;Ii<PORT_SIZE(wb_clk_i);Ii++)
    {
        if( INPUT_STATE(wb_clk_i[Ii])==ZERO )
        {
            temp_wb_clk_i[Ii]=0;            }
        else
        {
            temp_wb_clk_i[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(wb_rst_i);Ii++)
    {
        if( INPUT_STATE(wb_rst_i[Ii])==ZERO )
        {
            temp_wb_rst_i[Ii]=0;            }
        else
        {
            temp_wb_rst_i[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(arst_i);Ii++)
    {
        if( INPUT_STATE(arst_i[Ii])==ZERO )
        {
            temp_arst_i[Ii]=0;            }
        else
        {
            temp_arst_i[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(wb_adr_i);Ii++)
    {
        if( INPUT_STATE(wb_adr_i[Ii])==ZERO )
        {
            temp_wb_adr_i[Ii]=0;            }
        else
        {
            temp_wb_adr_i[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(wb_dat_i);Ii++)
    {
        if( INPUT_STATE(wb_dat_i[Ii])==ZERO )
        {
            temp_wb_dat_i[Ii]=0;            }
        else
        {
            temp_wb_dat_i[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(wb_we_i);Ii++)
    {
        if( INPUT_STATE(wb_we_i[Ii])==ZERO )
        {
            temp_wb_we_i[Ii]=0;            }
        else
        {
            temp_wb_we_i[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(wb_stb_i);Ii++)
    {
        if( INPUT_STATE(wb_stb_i[Ii])==ZERO )
        {
            temp_wb_stb_i[Ii]=0;            }
        else
        {
            temp_wb_stb_i[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(wb_cyc_i);Ii++)
    {
        if( INPUT_STATE(wb_cyc_i[Ii])==ZERO )
        {
            temp_wb_cyc_i[Ii]=0;            }
        else
        {
            temp_wb_cyc_i[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(scl_pad_i);Ii++)
    {
        if( INPUT_STATE(scl_pad_i[Ii])==ZERO )
        {
            temp_scl_pad_i[Ii]=0;            }
        else
        {
            temp_scl_pad_i[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(sda_pad_i);Ii++)
    {
        if( INPUT_STATE(sda_pad_i[Ii])==ZERO )
        {
            temp_sda_pad_i[Ii]=0;            }
        else
        {
            temp_sda_pad_i[Ii]=1;
        }
            }
	fooi2c_master_top(1,count);

	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(wb_dat_o);Ii++)
    {
        if(temp_wb_dat_o[Ii]==0)
        {
            _op_wb_dat_o[Ii]=ZERO;
            }
        else if(temp_wb_dat_o[Ii]==1)
        {
            _op_wb_dat_o[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(wb_dat_o[Ii]) = _op_wb_dat_o[Ii];
            }
        else if(_op_wb_dat_o[Ii] != _op_wb_dat_o_old[Ii])
        {
            OUTPUT_STATE(wb_dat_o[Ii]) = _op_wb_dat_o[Ii];
            OUTPUT_DELAY(wb_dat_o[Ii]) = ((_op_wb_dat_o[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(wb_dat_o[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(wb_dat_o[Ii]) = STRONG;
    }
	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(wb_ack_o);Ii++)
    {
        if(temp_wb_ack_o[Ii]==0)
        {
            _op_wb_ack_o[Ii]=ZERO;
            }
        else if(temp_wb_ack_o[Ii]==1)
        {
            _op_wb_ack_o[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(wb_ack_o[Ii]) = _op_wb_ack_o[Ii];
            }
        else if(_op_wb_ack_o[Ii] != _op_wb_ack_o_old[Ii])
        {
            OUTPUT_STATE(wb_ack_o[Ii]) = _op_wb_ack_o[Ii];
            OUTPUT_DELAY(wb_ack_o[Ii]) = ((_op_wb_ack_o[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(wb_ack_o[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(wb_ack_o[Ii]) = STRONG;
    }
	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(wb_inta_o);Ii++)
    {
        if(temp_wb_inta_o[Ii]==0)
        {
            _op_wb_inta_o[Ii]=ZERO;
            }
        else if(temp_wb_inta_o[Ii]==1)
        {
            _op_wb_inta_o[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(wb_inta_o[Ii]) = _op_wb_inta_o[Ii];
            }
        else if(_op_wb_inta_o[Ii] != _op_wb_inta_o_old[Ii])
        {
            OUTPUT_STATE(wb_inta_o[Ii]) = _op_wb_inta_o[Ii];
            OUTPUT_DELAY(wb_inta_o[Ii]) = ((_op_wb_inta_o[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(wb_inta_o[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(wb_inta_o[Ii]) = STRONG;
    }
	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(scl_pad_o);Ii++)
    {
        if(temp_scl_pad_o[Ii]==0)
        {
            _op_scl_pad_o[Ii]=ZERO;
            }
        else if(temp_scl_pad_o[Ii]==1)
        {
            _op_scl_pad_o[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(scl_pad_o[Ii]) = _op_scl_pad_o[Ii];
            }
        else if(_op_scl_pad_o[Ii] != _op_scl_pad_o_old[Ii])
        {
            OUTPUT_STATE(scl_pad_o[Ii]) = _op_scl_pad_o[Ii];
            OUTPUT_DELAY(scl_pad_o[Ii]) = ((_op_scl_pad_o[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(scl_pad_o[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(scl_pad_o[Ii]) = STRONG;
    }
	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(scl_padoen_o);Ii++)
    {
        if(temp_scl_padoen_o[Ii]==0)
        {
            _op_scl_padoen_o[Ii]=ZERO;
            }
        else if(temp_scl_padoen_o[Ii]==1)
        {
            _op_scl_padoen_o[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(scl_padoen_o[Ii]) = _op_scl_padoen_o[Ii];
            }
        else if(_op_scl_padoen_o[Ii] != _op_scl_padoen_o_old[Ii])
        {
            OUTPUT_STATE(scl_padoen_o[Ii]) = _op_scl_padoen_o[Ii];
            OUTPUT_DELAY(scl_padoen_o[Ii]) = ((_op_scl_padoen_o[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(scl_padoen_o[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(scl_padoen_o[Ii]) = STRONG;
    }
	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(sda_pad_o);Ii++)
    {
        if(temp_sda_pad_o[Ii]==0)
        {
            _op_sda_pad_o[Ii]=ZERO;
            }
        else if(temp_sda_pad_o[Ii]==1)
        {
            _op_sda_pad_o[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(sda_pad_o[Ii]) = _op_sda_pad_o[Ii];
            }
        else if(_op_sda_pad_o[Ii] != _op_sda_pad_o_old[Ii])
        {
            OUTPUT_STATE(sda_pad_o[Ii]) = _op_sda_pad_o[Ii];
            OUTPUT_DELAY(sda_pad_o[Ii]) = ((_op_sda_pad_o[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(sda_pad_o[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(sda_pad_o[Ii]) = STRONG;
    }
	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(sda_padoen_o);Ii++)
    {
        if(temp_sda_padoen_o[Ii]==0)
        {
            _op_sda_padoen_o[Ii]=ZERO;
            }
        else if(temp_sda_padoen_o[Ii]==1)
        {
            _op_sda_padoen_o[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(sda_padoen_o[Ii]) = _op_sda_padoen_o[Ii];
            }
        else if(_op_sda_padoen_o[Ii] != _op_sda_padoen_o_old[Ii])
        {
            OUTPUT_STATE(sda_padoen_o[Ii]) = _op_sda_padoen_o[Ii];
            OUTPUT_DELAY(sda_padoen_o[Ii]) = ((_op_sda_padoen_o[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(sda_padoen_o[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(sda_padoen_o[Ii]) = STRONG;
    }

}