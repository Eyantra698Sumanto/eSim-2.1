/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

                
        #include <stdio.h>
        #include <math.h>
        #include <string.h>
        #include "sim_main_cache.h"

        
void cm_cache(ARGS) 
{
	Digital_State_t *_op_out, *_op_out_old;
	Digital_State_t *_op_hit, *_op_hit_old;
	Digital_State_t *_op_miss, *_op_miss_old;

    static int inst_count=0;
    int count=0;
        
    if(INIT)
    {   
        inst_count++;
        PARAM(instance_id)=inst_count;
        foocache(0,inst_count);
        /* Allocate storage for output ports and set the load for input ports */

        
        port_address=PORT_SIZE(address);

        port_set=PORT_SIZE(set);

        port_tag=PORT_SIZE(tag);

        port_offset=PORT_SIZE(offset);

        port_index=PORT_SIZE(index);

        port_selection_signal=PORT_SIZE(selection_signal);

        port_clk=PORT_SIZE(clk);

        port_reset=PORT_SIZE(reset);

        port_data_in=PORT_SIZE(data_in);

        port_write=PORT_SIZE(write);

        port_out=PORT_SIZE(out);

        port_hit=PORT_SIZE(hit);

        port_miss=PORT_SIZE(miss);
		cm_event_alloc(0,32*sizeof(Digital_State_t));
		cm_event_alloc(1,1*sizeof(Digital_State_t));
		cm_event_alloc(2,1*sizeof(Digital_State_t));
		/* set the load for input ports. */
		int Ii;
		for(Ii=0;Ii<PORT_SIZE(address);Ii++)
		{
			LOAD(address[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(set);Ii++)
		{
			LOAD(set[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(tag);Ii++)
		{
			LOAD(tag[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(offset);Ii++)
		{
			LOAD(offset[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(index);Ii++)
		{
			LOAD(index[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(selection_signal);Ii++)
		{
			LOAD(selection_signal[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(clk);Ii++)
		{
			LOAD(clk[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(reset);Ii++)
		{
			LOAD(reset[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(data_in);Ii++)
		{
			LOAD(data_in[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(write);Ii++)
		{
			LOAD(write[Ii])=PARAM(input_load); 
		}

		/*Retrieve Storage for output*/
		_op_out = _op_out_old = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_hit = _op_hit_old = (Digital_State_t *) cm_event_get_ptr(1,0);
		_op_miss = _op_miss_old = (Digital_State_t *) cm_event_get_ptr(2,0);


	}
	else
	{
		_op_out = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_out_old = (Digital_State_t *) cm_event_get_ptr(0,1);
		_op_hit = (Digital_State_t *) cm_event_get_ptr(1,1);
		_op_hit_old = (Digital_State_t *) cm_event_get_ptr(1,2);
		_op_miss = (Digital_State_t *) cm_event_get_ptr(2,2);
		_op_miss_old = (Digital_State_t *) cm_event_get_ptr(2,3);
	}

	//Formating data for sending it to client
	int Ii;
	count=(int)PARAM(instance_id);

    for(Ii=0;Ii<PORT_SIZE(address);Ii++)
    {
        if( INPUT_STATE(address[Ii])==ZERO )
        {
            temp_address[Ii]=0;            }
        else
        {
            temp_address[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(set);Ii++)
    {
        if( INPUT_STATE(set[Ii])==ZERO )
        {
            temp_set[Ii]=0;            }
        else
        {
            temp_set[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(tag);Ii++)
    {
        if( INPUT_STATE(tag[Ii])==ZERO )
        {
            temp_tag[Ii]=0;            }
        else
        {
            temp_tag[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(offset);Ii++)
    {
        if( INPUT_STATE(offset[Ii])==ZERO )
        {
            temp_offset[Ii]=0;            }
        else
        {
            temp_offset[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(index);Ii++)
    {
        if( INPUT_STATE(index[Ii])==ZERO )
        {
            temp_index[Ii]=0;            }
        else
        {
            temp_index[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(selection_signal);Ii++)
    {
        if( INPUT_STATE(selection_signal[Ii])==ZERO )
        {
            temp_selection_signal[Ii]=0;            }
        else
        {
            temp_selection_signal[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(clk);Ii++)
    {
        if( INPUT_STATE(clk[Ii])==ZERO )
        {
            temp_clk[Ii]=0;            }
        else
        {
            temp_clk[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(reset);Ii++)
    {
        if( INPUT_STATE(reset[Ii])==ZERO )
        {
            temp_reset[Ii]=0;            }
        else
        {
            temp_reset[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(data_in);Ii++)
    {
        if( INPUT_STATE(data_in[Ii])==ZERO )
        {
            temp_data_in[Ii]=0;            }
        else
        {
            temp_data_in[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(write);Ii++)
    {
        if( INPUT_STATE(write[Ii])==ZERO )
        {
            temp_write[Ii]=0;            }
        else
        {
            temp_write[Ii]=1;
        }
            }
	foocache(1,count);

	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(out);Ii++)
    {
        if(temp_out[Ii]==0)
        {
            _op_out[Ii]=ZERO;
            }
        else if(temp_out[Ii]==1)
        {
            _op_out[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(out[Ii]) = _op_out[Ii];
            }
        else if(_op_out[Ii] != _op_out_old[Ii])
        {
            OUTPUT_STATE(out[Ii]) = _op_out[Ii];
            OUTPUT_DELAY(out[Ii]) = ((_op_out[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(out[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(out[Ii]) = STRONG;
    }
	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(hit);Ii++)
    {
        if(temp_hit[Ii]==0)
        {
            _op_hit[Ii]=ZERO;
            }
        else if(temp_hit[Ii]==1)
        {
            _op_hit[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(hit[Ii]) = _op_hit[Ii];
            }
        else if(_op_hit[Ii] != _op_hit_old[Ii])
        {
            OUTPUT_STATE(hit[Ii]) = _op_hit[Ii];
            OUTPUT_DELAY(hit[Ii]) = ((_op_hit[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(hit[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(hit[Ii]) = STRONG;
    }
	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(miss);Ii++)
    {
        if(temp_miss[Ii]==0)
        {
            _op_miss[Ii]=ZERO;
            }
        else if(temp_miss[Ii]==1)
        {
            _op_miss[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(miss[Ii]) = _op_miss[Ii];
            }
        else if(_op_miss[Ii] != _op_miss_old[Ii])
        {
            OUTPUT_STATE(miss[Ii]) = _op_miss[Ii];
            OUTPUT_DELAY(miss[Ii]) = ((_op_miss[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(miss[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(miss[Ii]) = STRONG;
    }

}