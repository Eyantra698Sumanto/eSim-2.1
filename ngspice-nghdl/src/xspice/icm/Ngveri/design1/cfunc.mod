/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

                
        #include <stdio.h>
        #include <math.h>
        #include <string.h>
        #include "sim_main.h"

        
void cm_design1(ARGS) 
{
	Digital_State_t *_op_Y, *_op_Y_old;

    static int inst_count=0;
    int count=0;
        
    if(INIT)
    {   
        inst_count++;
        PARAM(instance_id)=inst_count;
        foo(0,inst_count);
        /* Allocate storage for output ports and set the load for input ports */

        
        port_D0=PORT_SIZE(D0);

        port_D1=PORT_SIZE(D1);

        port_S=PORT_SIZE(S);

        port_Y=PORT_SIZE(Y);
		cm_event_alloc(0,1*sizeof(Digital_State_t));
		/* set the load for input ports. */
		int Ii;
		for(Ii=0;Ii<PORT_SIZE(D0);Ii++)
		{
			LOAD(D0[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(D1);Ii++)
		{
			LOAD(D1[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(S);Ii++)
		{
			LOAD(S[Ii])=PARAM(input_load); 
		}

		/*Retrieve Storage for output*/
		_op_Y = _op_Y_old = (Digital_State_t *) cm_event_get_ptr(0,0);


	}
	else
	{
		_op_Y = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_Y_old = (Digital_State_t *) cm_event_get_ptr(0,1);
	}

	//Formating data for sending it to client
	int Ii;
	count=(int)PARAM(instance_id);

    for(Ii=0;Ii<PORT_SIZE(D0);Ii++)
    {
        if( INPUT_STATE(D0[Ii])==ZERO )
        {
            temp_D0[Ii]=0;            }
        else
        {
            temp_D0[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(D1);Ii++)
    {
        if( INPUT_STATE(D1[Ii])==ZERO )
        {
            temp_D1[Ii]=0;            }
        else
        {
            temp_D1[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(S);Ii++)
    {
        if( INPUT_STATE(S[Ii])==ZERO )
        {
            temp_S[Ii]=0;            }
        else
        {
            temp_S[Ii]=1;
        }
            }
	foo(1,count);

	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(Y);Ii++)
    {
        if(temp_Y[Ii]==0)
        {
            _op_Y[Ii]=ZERO;
            }
        else if(temp_Y[Ii]==1)
        {
            _op_Y[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(Y[Ii]) = _op_Y[Ii];
            }
        else if(_op_Y[Ii] != _op_Y_old[Ii])
        {
            OUTPUT_STATE(Y[Ii]) = _op_Y[Ii];
            OUTPUT_DELAY(Y[Ii]) = ((_op_Y[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(Y[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(Y[Ii]) = STRONG;
    }

}