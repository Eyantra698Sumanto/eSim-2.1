/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

        
        #include <memory>
        #include <verilated.h>
        #include "Vdivision.h"
        #include <stdio.h>
        #include <stdio.h>
        #include <fstream>
        #include <stdlib.h>
        #include <string>
        #include <iostream>
        #include <cstring>
        using namespace std;
        
        extern "C" int temp_a[1024];
        extern "C" int port_a;
        extern "C" int temp_b[1024];
        extern "C" int port_b;
        extern "C" int temp_exception[1024];
        extern "C" int port_exception;
        extern "C" int temp_res[1024];
        extern "C" int port_res;
        extern "C" int foodivision(int,int);
        
        void int2arrdivision(int  num, int array[], int n)
        {   
            for (int i = 0; i < n && num>=0; i++) 
            {
                array[n-i-1] = num % 2;
                num /= 2;
                }
        }
        int arr2intdivision(int array[],int n)
        {   
            int i,k=0;
            for (i = 0; i < n; i++) 
                k = 2 * k + array[i];
            return k;
        }
        
        int foodivision(int init,int count) 
        {
            static VerilatedContext* contextp = new VerilatedContext;
            static Vdivision* division[1024];
            count--;
            if (init==0) 
            {
                division[count]=new Vdivision{contextp};
                contextp->traceEverOn(true);
            }
            else
            {
                contextp->timeInc(1);
                printf("=============division : New Iteration===========");
                printf("\nInstance : %d\n",count);
                printf("\nInside foo before eval.....\n");
				printf("a=%d\n", division[count] ->a);
				printf("b=%d\n", division[count] ->b);
				printf("exception=%d\n", division[count] ->exception);
				printf("res=%d\n", division[count] ->res);
				division[count]->a = arr2intdivision(temp_a, port_a);
				division[count]->b = arr2intdivision(temp_b, port_b);
				division[count]->eval();

                printf("\nInside foo after eval.....\n");
				printf("a=%d\n", division[count] ->a);
				printf("b=%d\n", division[count] ->b);
				printf("exception=%d\n", division[count] ->exception);
				printf("res=%d\n", division[count] ->res);
				int2arrdivision(division[count] -> exception, temp_exception, port_exception);
				int2arrdivision(division[count] -> res, temp_res, port_res);

            }
            return 0;
        }