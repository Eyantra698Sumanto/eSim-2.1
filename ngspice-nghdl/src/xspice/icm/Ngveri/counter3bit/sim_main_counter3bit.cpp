/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

        
        #include <memory>
        #include <verilated.h>
        #include "Vcounter3bit.h"
        #include <stdio.h>
        #include <stdio.h>
        #include <fstream>
        #include <stdlib.h>
        #include <string>
        #include <iostream>
        #include <cstring>
        using namespace std;
        
        extern "C" int temp_clk[1024];
        extern "C" int port_clk;
        extern "C" int temp_rst[1024];
        extern "C" int port_rst;
        extern "C" int temp_out[1024];
        extern "C" int port_out;
        extern "C" int foocounter3bit(int,int);
        
        void int2arrcounter3bit(int  num, int array[], int n)
        {   
            for (int i = 0; i < n && num>=0; i++) 
            {
                array[n-i-1] = num % 2;
                num /= 2;
                }
        }
        int arr2intcounter3bit(int array[],int n)
        {   
            int i,k=0;
            for (i = 0; i < n; i++) 
                k = 2 * k + array[i];
            return k;
        }
        
        int foocounter3bit(int init,int count) 
        {
            static VerilatedContext* contextp = new VerilatedContext;
            static Vcounter3bit* counter3bit[1024];
            count--;
            if (init==0) 
            {
                counter3bit[count]=new Vcounter3bit{contextp};
                contextp->traceEverOn(true);
            }
            else
            {
                contextp->timeInc(1);
                printf("=============counter3bit : New Iteration===========");
                printf("\nInstance : %d\n",count);
                printf("\nInside foo before eval.....\n");
				printf("clk=%d\n", counter3bit[count] ->clk);
				printf("rst=%d\n", counter3bit[count] ->rst);
				printf("out=%d\n", counter3bit[count] ->out);
				counter3bit[count]->clk = arr2intcounter3bit(temp_clk, port_clk);
				counter3bit[count]->rst = arr2intcounter3bit(temp_rst, port_rst);
				counter3bit[count]->eval();

                printf("\nInside foo after eval.....\n");
				printf("clk=%d\n", counter3bit[count] ->clk);
				printf("rst=%d\n", counter3bit[count] ->rst);
				printf("out=%d\n", counter3bit[count] ->out);
				int2arrcounter3bit(counter3bit[count] -> out, temp_out, port_out);

            }
            return 0;
        }