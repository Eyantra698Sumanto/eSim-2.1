/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

        
        #include <memory>
        #include <verilated.h>
        #include "Vjbuffer.h"
        #include <stdio.h>
        #include <stdio.h>
        #include <fstream>
        #include <stdlib.h>
        #include <string>
        #include <iostream>
        #include <cstring>
        using namespace std;
        
        extern "C" int temp_a[1024];
        extern "C" int port_a;
        extern "C" int temp_y[1024];
        extern "C" int port_y;
        extern "C" int foojbuffer(int,int);
        
        void int2arrjbuffer(int  num, int array[], int n)
        {   
            for (int i = 0; i < n && num>=0; i++) 
            {
                array[n-i-1] = num % 2;
                num /= 2;
                }
        }
        int arr2intjbuffer(int array[],int n)
        {   
            int i,k=0;
            for (i = 0; i < n; i++) 
                k = 2 * k + array[i];
            return k;
        }
        
        int foojbuffer(int init,int count) 
        {
            static VerilatedContext* contextp = new VerilatedContext;
            static Vjbuffer* jbuffer[1024];
            count--;
            if (init==0) 
            {
                jbuffer[count]=new Vjbuffer{contextp};
                contextp->traceEverOn(true);
            }
            else
            {
                contextp->timeInc(1);
                printf("=============jbuffer : New Iteration===========");
                printf("\nInstance : %d\n",count);
                printf("\nInside foo before eval.....\n");
				printf("a=%d\n", jbuffer[count] ->a);
				printf("y=%d\n", jbuffer[count] ->y);
				jbuffer[count]->a = arr2intjbuffer(temp_a, port_a);
				jbuffer[count]->eval();

                printf("\nInside foo after eval.....\n");
				printf("a=%d\n", jbuffer[count] ->a);
				printf("y=%d\n", jbuffer[count] ->y);
				int2arrjbuffer(jbuffer[count] -> y, temp_y, port_y);

            }
            return 0;
        }