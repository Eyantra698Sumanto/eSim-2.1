/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

                
        #include <stdio.h>
        #include <math.h>
        #include <string.h>
        #include "sim_main_parallel_crc.h"

        
void cm_parallel_crc(ARGS) 
{
	Digital_State_t *_op_crc_out, *_op_crc_out_old;

    static int inst_count=0;
    int count=0;
        
    if(INIT)
    {   
        inst_count++;
        PARAM(instance_id)=inst_count;
        fooparallel_crc(0,inst_count);
        /* Allocate storage for output ports and set the load for input ports */

        
        port_clk=PORT_SIZE(clk);

        port_reset=PORT_SIZE(reset);

        port_enable=PORT_SIZE(enable);

        port_init=PORT_SIZE(init);

        port_data_in=PORT_SIZE(data_in);

        port_crc_out=PORT_SIZE(crc_out);
		cm_event_alloc(0,16*sizeof(Digital_State_t));
		/* set the load for input ports. */
		int Ii;
		for(Ii=0;Ii<PORT_SIZE(clk);Ii++)
		{
			LOAD(clk[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(reset);Ii++)
		{
			LOAD(reset[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(enable);Ii++)
		{
			LOAD(enable[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(init);Ii++)
		{
			LOAD(init[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(data_in);Ii++)
		{
			LOAD(data_in[Ii])=PARAM(input_load); 
		}

		/*Retrieve Storage for output*/
		_op_crc_out = _op_crc_out_old = (Digital_State_t *) cm_event_get_ptr(0,0);


	}
	else
	{
		_op_crc_out = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_crc_out_old = (Digital_State_t *) cm_event_get_ptr(0,1);
	}

	//Formating data for sending it to client
	int Ii;
	count=(int)PARAM(instance_id);

    for(Ii=0;Ii<PORT_SIZE(clk);Ii++)
    {
        if( INPUT_STATE(clk[Ii])==ZERO )
        {
            temp_clk[Ii]=0;            }
        else
        {
            temp_clk[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(reset);Ii++)
    {
        if( INPUT_STATE(reset[Ii])==ZERO )
        {
            temp_reset[Ii]=0;            }
        else
        {
            temp_reset[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(enable);Ii++)
    {
        if( INPUT_STATE(enable[Ii])==ZERO )
        {
            temp_enable[Ii]=0;            }
        else
        {
            temp_enable[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(init);Ii++)
    {
        if( INPUT_STATE(init[Ii])==ZERO )
        {
            temp_init[Ii]=0;            }
        else
        {
            temp_init[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(data_in);Ii++)
    {
        if( INPUT_STATE(data_in[Ii])==ZERO )
        {
            temp_data_in[Ii]=0;            }
        else
        {
            temp_data_in[Ii]=1;
        }
            }
	fooparallel_crc(1,count);

	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(crc_out);Ii++)
    {
        if(temp_crc_out[Ii]==0)
        {
            _op_crc_out[Ii]=ZERO;
            }
        else if(temp_crc_out[Ii]==1)
        {
            _op_crc_out[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(crc_out[Ii]) = _op_crc_out[Ii];
            }
        else if(_op_crc_out[Ii] != _op_crc_out_old[Ii])
        {
            OUTPUT_STATE(crc_out[Ii]) = _op_crc_out[Ii];
            OUTPUT_DELAY(crc_out[Ii]) = ((_op_crc_out[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(crc_out[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(crc_out[Ii]) = STRONG;
    }

}