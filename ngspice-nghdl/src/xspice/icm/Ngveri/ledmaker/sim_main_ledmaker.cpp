/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

        
        #include <memory>
        #include <verilated.h>
        #include "Vledmaker.h"
        #include <stdio.h>
        #include <stdio.h>
        #include <fstream>
        #include <stdlib.h>
        #include <string>
        #include <iostream>
        #include <cstring>
        using namespace std;
        
        extern "C" int temp_clk[1024];
        extern "C" int port_clk;
        extern "C" int temp_reset[1024];
        extern "C" int port_reset;
        extern "C" int temp_cyc_cnt[1024];
        extern "C" int port_cyc_cnt;
        extern "C" int temp_passed[1024];
        extern "C" int port_passed;
        extern "C" int temp_failed[1024];
        extern "C" int port_failed;
        extern "C" int fooledmaker(int,int);
        
        void int2arrledmaker(int  num, int array[], int n)
        {   
            for (int i = 0; i < n && num>=0; i++) 
            {
                array[n-i-1] = num % 2;
                num /= 2;
                }
        }
        int arr2intledmaker(int array[],int n)
        {   
            int i,k=0;
            for (i = 0; i < n; i++) 
                k = 2 * k + array[i];
            return k;
        }
        
        int fooledmaker(int init,int count) 
        {
            static VerilatedContext* contextp = new VerilatedContext;
            static Vledmaker* ledmaker[1024];
            count--;
            if (init==0) 
            {
                ledmaker[count]=new Vledmaker{contextp};
                contextp->traceEverOn(true);
            }
            else
            {
                contextp->timeInc(1);
                printf("=============ledmaker : New Iteration===========");
                printf("\nInstance : %d\n",count);
                printf("\nInside foo before eval.....\n");
				printf("clk=%d\n", ledmaker[count] ->clk);
				printf("reset=%d\n", ledmaker[count] ->reset);
				printf("cyc_cnt=%d\n", ledmaker[count] ->cyc_cnt);
				printf("passed=%d\n", ledmaker[count] ->passed);
				printf("failed=%d\n", ledmaker[count] ->failed);
				ledmaker[count]->clk = arr2intledmaker(temp_clk, port_clk);
				ledmaker[count]->reset = arr2intledmaker(temp_reset, port_reset);
				ledmaker[count]->cyc_cnt = arr2intledmaker(temp_cyc_cnt, port_cyc_cnt);
				ledmaker[count]->eval();

                printf("\nInside foo after eval.....\n");
				printf("clk=%d\n", ledmaker[count] ->clk);
				printf("reset=%d\n", ledmaker[count] ->reset);
				printf("cyc_cnt=%d\n", ledmaker[count] ->cyc_cnt);
				printf("passed=%d\n", ledmaker[count] ->passed);
				printf("failed=%d\n", ledmaker[count] ->failed);
				int2arrledmaker(ledmaker[count] -> passed, temp_passed, port_passed);
				int2arrledmaker(ledmaker[count] -> failed, temp_failed, port_failed);

            }
            return 0;
        }