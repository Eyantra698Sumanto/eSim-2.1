/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

        
        #include <memory>
        #include <verilated.h>
        #include "Vtop.h"
        #include <stdio.h>
        #include <stdio.h>
        #include <fstream>
        #include <stdlib.h>
        #include <string>
        #include <iostream>
        #include <cstring>
        using namespace std;
        
        extern "C" int temp_clk[1024];
        extern "C" int port_clk;
        extern "C" int temp_reset[1024];
        extern "C" int port_reset;
        extern "C" int temp_cyc_cnt[1024];
        extern "C" int port_cyc_cnt;
        extern "C" int temp_passed[1024];
        extern "C" int port_passed;
        extern "C" int temp_failed[1024];
        extern "C" int port_failed;
        extern "C" int footop(int,int);
        
        void int2arrtop(int  num, int array[], int n)
        {   
            for (int i = 0; i < n && num>=0; i++) 
            {
                array[n-i-1] = num % 2;
                num /= 2;
                }
        }
        int arr2inttop(int array[],int n)
        {   
            int i,k=0;
            for (i = 0; i < n; i++) 
                k = 2 * k + array[i];
            return k;
        }
        
        int footop(int init,int count) 
        {
            static VerilatedContext* contextp = new VerilatedContext;
            static Vtop* top[1024];
            count--;
            if (init==0) 
            {
                top[count]=new Vtop{contextp};
                contextp->traceEverOn(true);
            }
            else
            {
                contextp->timeInc(1);
                printf("=============New Iteration===========");
                printf("\nInside foo before eval.....\n");
				printf("clk=%d\n", top[count] ->clk);
				printf("reset=%d\n", top[count] ->reset);
				printf("cyc_cnt=%d\n", top[count] ->cyc_cnt);
				printf("passed=%d\n", top[count] ->passed);
				printf("failed=%d\n", top[count] ->failed);
				top[count]->clk = arr2inttop(temp_clk, port_clk);
				top[count]->reset = arr2inttop(temp_reset, port_reset);
				top[count]->cyc_cnt = arr2inttop(temp_cyc_cnt, port_cyc_cnt);
				top[count]->eval();

                printf("\nInside foo after eval.....\n");
				printf("clk=%d\n", top[count] ->clk);
				printf("reset=%d\n", top[count] ->reset);
				printf("cyc_cnt=%d\n", top[count] ->cyc_cnt);
				printf("passed=%d\n", top[count] ->passed);
				printf("failed=%d\n", top[count] ->failed);
				int2arrtop(top[count] -> passed, temp_passed, port_passed);
				int2arrtop(top[count] -> failed, temp_failed, port_failed);

            }
            return 0;
        }