/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

        
        #include <memory>
        #include <verilated.h>
        #include "Vcounter4bit.h"
        #include <stdio.h>
        #include <stdio.h>
        #include <fstream>
        #include <stdlib.h>
        #include <string>
        #include <iostream>
        #include <cstring>
        using namespace std;
        
        extern "C" int temp_clk[1024];
        extern "C" int port_clk;
        extern "C" int temp_rstn[1024];
        extern "C" int port_rstn;
        extern "C" int temp_out[1024];
        extern "C" int port_out;
        extern "C" int foocounter4bit(int,int);
        
        void int2arrcounter4bit(int  num, int array[], int n)
        {   
            for (int i = 0; i < n && num>=0; i++) 
            {
                array[n-i-1] = num % 2;
                num /= 2;
                }
        }
        int arr2intcounter4bit(int array[],int n)
        {   
            int i,k=0;
            for (i = 0; i < n; i++) 
                k = 2 * k + array[i];
            return k;
        }
        
        int foocounter4bit(int init,int count) 
        {
            static VerilatedContext* contextp = new VerilatedContext;
            static Vcounter4bit* counter4bit[1024];
            count--;
            if (init==0) 
            {
                counter4bit[count]=new Vcounter4bit{contextp};
                contextp->traceEverOn(true);
            }
            else
            {
                contextp->timeInc(1);
                printf("=============counter4bit : New Iteration===========");
                printf("\nInstance : %d\n",count);
                printf("\nInside foo before eval.....\n");
				printf("clk=%d\n", counter4bit[count] ->clk);
				printf("rstn=%d\n", counter4bit[count] ->rstn);
				printf("out=%d\n", counter4bit[count] ->out);
				counter4bit[count]->clk = arr2intcounter4bit(temp_clk, port_clk);
				counter4bit[count]->rstn = arr2intcounter4bit(temp_rstn, port_rstn);
				counter4bit[count]->eval();

                printf("\nInside foo after eval.....\n");
				printf("clk=%d\n", counter4bit[count] ->clk);
				printf("rstn=%d\n", counter4bit[count] ->rstn);
				printf("out=%d\n", counter4bit[count] ->out);
				int2arrcounter4bit(counter4bit[count] -> out, temp_out, port_out);

            }
            return 0;
        }