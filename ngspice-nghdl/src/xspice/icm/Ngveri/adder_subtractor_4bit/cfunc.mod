/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

                
        #include <stdio.h>
        #include <math.h>
        #include <string.h>
        #include "sim_main_adder_subtractor_4bit.h"

        
void cm_adder_subtractor_4bit(ARGS) 
{
	Digital_State_t *_op_sum, *_op_sum_old;
	Digital_State_t *_op_carry, *_op_carry_old;

    static int inst_count=0;
    int count=0;
        
    if(INIT)
    {   
        inst_count++;
        PARAM(instance_id)=inst_count;
        fooadder_subtractor_4bit(0,inst_count);
        /* Allocate storage for output ports and set the load for input ports */

        
        port_a=PORT_SIZE(a);

        port_b=PORT_SIZE(b);

        port_cin=PORT_SIZE(cin);

        port_sum=PORT_SIZE(sum);

        port_carry=PORT_SIZE(carry);
		cm_event_alloc(0,4*sizeof(Digital_State_t));
		cm_event_alloc(1,1*sizeof(Digital_State_t));
		/* set the load for input ports. */
		int Ii;
		for(Ii=0;Ii<PORT_SIZE(a);Ii++)
		{
			LOAD(a[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(b);Ii++)
		{
			LOAD(b[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(cin);Ii++)
		{
			LOAD(cin[Ii])=PARAM(input_load); 
		}

		/*Retrieve Storage for output*/
		_op_sum = _op_sum_old = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_carry = _op_carry_old = (Digital_State_t *) cm_event_get_ptr(1,0);


	}
	else
	{
		_op_sum = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_sum_old = (Digital_State_t *) cm_event_get_ptr(0,1);
		_op_carry = (Digital_State_t *) cm_event_get_ptr(1,1);
		_op_carry_old = (Digital_State_t *) cm_event_get_ptr(1,2);
	}

	//Formating data for sending it to client
	int Ii;
	count=(int)PARAM(instance_id);

    for(Ii=0;Ii<PORT_SIZE(a);Ii++)
    {
        if( INPUT_STATE(a[Ii])==ZERO )
        {
            temp_a[Ii]=0;            }
        else
        {
            temp_a[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(b);Ii++)
    {
        if( INPUT_STATE(b[Ii])==ZERO )
        {
            temp_b[Ii]=0;            }
        else
        {
            temp_b[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(cin);Ii++)
    {
        if( INPUT_STATE(cin[Ii])==ZERO )
        {
            temp_cin[Ii]=0;            }
        else
        {
            temp_cin[Ii]=1;
        }
            }
	fooadder_subtractor_4bit(1,count);

	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(sum);Ii++)
    {
        if(temp_sum[Ii]==0)
        {
            _op_sum[Ii]=ZERO;
            }
        else if(temp_sum[Ii]==1)
        {
            _op_sum[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(sum[Ii]) = _op_sum[Ii];
            }
        else if(_op_sum[Ii] != _op_sum_old[Ii])
        {
            OUTPUT_STATE(sum[Ii]) = _op_sum[Ii];
            OUTPUT_DELAY(sum[Ii]) = ((_op_sum[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(sum[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(sum[Ii]) = STRONG;
    }
	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(carry);Ii++)
    {
        if(temp_carry[Ii]==0)
        {
            _op_carry[Ii]=ZERO;
            }
        else if(temp_carry[Ii]==1)
        {
            _op_carry[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(carry[Ii]) = _op_carry[Ii];
            }
        else if(_op_carry[Ii] != _op_carry_old[Ii])
        {
            OUTPUT_STATE(carry[Ii]) = _op_carry[Ii];
            OUTPUT_DELAY(carry[Ii]) = ((_op_carry[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(carry[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(carry[Ii]) = STRONG;
    }

}