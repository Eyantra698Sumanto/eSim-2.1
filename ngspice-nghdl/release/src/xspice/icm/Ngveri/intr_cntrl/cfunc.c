#include "ngspice/cm.h"
extern void cm_intr_cntrl(Mif_Private_t *);
/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

                
        #include <stdio.h>
        #include <math.h>
        #include <string.h>
        #include "sim_main_intr_cntrl.h"

        
void cm_intr_cntrl(Mif_Private_t *mif_private) 
{
	Digital_State_t *_op_intr_out, *_op_intr_out_old;
	Digital_State_t *_op_bus_oe, *_op_bus_oe_old;

    static int inst_count=0;
    int count=0;
        
    if(mif_private->circuit.init)
    {   
        inst_count++;
        mif_private->param[0]->element[0].rvalue=inst_count;
        foointr_cntrl(0,inst_count);
        /* Allocate storage for output ports and set the load for input ports */

        
        port_clk_in=mif_private->conn[0]->size;

        port_rst_in=mif_private->conn[1]->size;

        port_intr_rq=mif_private->conn[2]->size;

        port_intr_bus=mif_private->conn[3]->size;

        port_intr_in=mif_private->conn[4]->size;

        port_intr_out=mif_private->conn[5]->size;

        port_bus_oe=mif_private->conn[6]->size;
		cm_event_alloc(0,1*sizeof(Digital_State_t));
		cm_event_alloc(1,1*sizeof(Digital_State_t));
		/* set the load for input ports. */
		int Ii;
		for(Ii=0;Ii<mif_private->conn[0]->size;Ii++)
		{
			mif_private->conn[0]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}
		for(Ii=0;Ii<mif_private->conn[1]->size;Ii++)
		{
			mif_private->conn[1]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}
		for(Ii=0;Ii<mif_private->conn[2]->size;Ii++)
		{
			mif_private->conn[2]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}
		for(Ii=0;Ii<mif_private->conn[3]->size;Ii++)
		{
			mif_private->conn[3]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}
		for(Ii=0;Ii<mif_private->conn[4]->size;Ii++)
		{
			mif_private->conn[4]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}

		/*Retrieve Storage for output*/
		_op_intr_out = _op_intr_out_old = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_bus_oe = _op_bus_oe_old = (Digital_State_t *) cm_event_get_ptr(1,0);


	}
	else
	{
		_op_intr_out = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_intr_out_old = (Digital_State_t *) cm_event_get_ptr(0,1);
		_op_bus_oe = (Digital_State_t *) cm_event_get_ptr(1,1);
		_op_bus_oe_old = (Digital_State_t *) cm_event_get_ptr(1,2);
	}

	//Formating data for sending it to client
	int Ii;
	count=(int)mif_private->param[0]->element[0].rvalue;

    for(Ii=0;Ii<mif_private->conn[0]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[0]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_clk_in[Ii]=0;            }
        else
        {
            temp_clk_in[Ii]=1;
        }
            }
    for(Ii=0;Ii<mif_private->conn[1]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[1]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_rst_in[Ii]=0;            }
        else
        {
            temp_rst_in[Ii]=1;
        }
            }
    for(Ii=0;Ii<mif_private->conn[2]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[2]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_intr_rq[Ii]=0;            }
        else
        {
            temp_intr_rq[Ii]=1;
        }
            }
    for(Ii=0;Ii<mif_private->conn[3]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[3]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_intr_bus[Ii]=0;            }
        else
        {
            temp_intr_bus[Ii]=1;
        }
            }
    for(Ii=0;Ii<mif_private->conn[4]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[4]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_intr_in[Ii]=0;            }
        else
        {
            temp_intr_in[Ii]=1;
        }
            }
	foointr_cntrl(1,count);

	/* Scheduling event and processing them */
    for(Ii=0;Ii<mif_private->conn[5]->size;Ii++)
    {
        if(temp_intr_out[Ii]==0)
        {
            _op_intr_out[Ii]=ZERO;
            }
        else if(temp_intr_out[Ii]==1)
        {
            _op_intr_out[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(mif_private->circuit.anal_type == DC)
        {
            ((Digital_t*)(mif_private->conn[5]->port[Ii]->output.pvalue))->state = _op_intr_out[Ii];
            }
        else if(_op_intr_out[Ii] != _op_intr_out_old[Ii])
        {
            ((Digital_t*)(mif_private->conn[5]->port[Ii]->output.pvalue))->state = _op_intr_out[Ii];
            mif_private->conn[5]->port[Ii]->delay = ((_op_intr_out[Ii] == ZERO) ? mif_private->param[3]->element[0].rvalue : mif_private->param[2]->element[0].rvalue);
            }
        else
        {
            mif_private->conn[5]->port[Ii]->changed = FALSE;
            }
        ((Digital_t*)(mif_private->conn[5]->port[Ii]->output.pvalue))->strength = STRONG;
    }
	/* Scheduling event and processing them */
    for(Ii=0;Ii<mif_private->conn[6]->size;Ii++)
    {
        if(temp_bus_oe[Ii]==0)
        {
            _op_bus_oe[Ii]=ZERO;
            }
        else if(temp_bus_oe[Ii]==1)
        {
            _op_bus_oe[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(mif_private->circuit.anal_type == DC)
        {
            ((Digital_t*)(mif_private->conn[6]->port[Ii]->output.pvalue))->state = _op_bus_oe[Ii];
            }
        else if(_op_bus_oe[Ii] != _op_bus_oe_old[Ii])
        {
            ((Digital_t*)(mif_private->conn[6]->port[Ii]->output.pvalue))->state = _op_bus_oe[Ii];
            mif_private->conn[6]->port[Ii]->delay = ((_op_bus_oe[Ii] == ZERO) ? mif_private->param[3]->element[0].rvalue : mif_private->param[2]->element[0].rvalue);
            }
        else
        {
            mif_private->conn[6]->port[Ii]->changed = FALSE;
            }
        ((Digital_t*)(mif_private->conn[6]->port[Ii]->output.pvalue))->strength = STRONG;
    }

}