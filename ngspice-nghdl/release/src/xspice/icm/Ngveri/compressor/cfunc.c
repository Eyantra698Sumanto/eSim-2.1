#include "ngspice/cm.h"
extern void cm_compressor(Mif_Private_t *);
/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

                
        #include <stdio.h>
        #include <math.h>
        #include <string.h>
        #include "sim_main_compressor.h"

        
void cm_compressor(Mif_Private_t *mif_private) 
{
	Digital_State_t *_op_CompressedCache, *_op_CompressedCache_old;
	Digital_State_t *_op_DeCompressedCache, *_op_DeCompressedCache_old;

    static int inst_count=0;
    int count=0;
        
    if(mif_private->circuit.init)
    {   
        inst_count++;
        mif_private->param[0]->element[0].rvalue=inst_count;
        foocompressor(0,inst_count);
        /* Allocate storage for output ports and set the load for input ports */

        
        port_clock=mif_private->conn[0]->size;

        port_UnCompressedCache=mif_private->conn[1]->size;

        port_h=mif_private->conn[2]->size;

        port_n=mif_private->conn[3]->size;

        port_CompressedCache=mif_private->conn[4]->size;

        port_DeCompressedCache=mif_private->conn[5]->size;
		cm_event_alloc(0,256*sizeof(Digital_State_t));
		cm_event_alloc(1,256*sizeof(Digital_State_t));
		/* set the load for input ports. */
		int Ii;
		for(Ii=0;Ii<mif_private->conn[0]->size;Ii++)
		{
			mif_private->conn[0]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}
		for(Ii=0;Ii<mif_private->conn[1]->size;Ii++)
		{
			mif_private->conn[1]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}
		for(Ii=0;Ii<mif_private->conn[2]->size;Ii++)
		{
			mif_private->conn[2]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}
		for(Ii=0;Ii<mif_private->conn[3]->size;Ii++)
		{
			mif_private->conn[3]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}

		/*Retrieve Storage for output*/
		_op_CompressedCache = _op_CompressedCache_old = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_DeCompressedCache = _op_DeCompressedCache_old = (Digital_State_t *) cm_event_get_ptr(1,0);


	}
	else
	{
		_op_CompressedCache = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_CompressedCache_old = (Digital_State_t *) cm_event_get_ptr(0,1);
		_op_DeCompressedCache = (Digital_State_t *) cm_event_get_ptr(1,1);
		_op_DeCompressedCache_old = (Digital_State_t *) cm_event_get_ptr(1,2);
	}

	//Formating data for sending it to client
	int Ii;
	count=(int)mif_private->param[0]->element[0].rvalue;

    for(Ii=0;Ii<mif_private->conn[0]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[0]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_clock[Ii]=0;            }
        else
        {
            temp_clock[Ii]=1;
        }
            }
    for(Ii=0;Ii<mif_private->conn[1]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[1]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_UnCompressedCache[Ii]=0;            }
        else
        {
            temp_UnCompressedCache[Ii]=1;
        }
            }
    for(Ii=0;Ii<mif_private->conn[2]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[2]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_h[Ii]=0;            }
        else
        {
            temp_h[Ii]=1;
        }
            }
    for(Ii=0;Ii<mif_private->conn[3]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[3]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_n[Ii]=0;            }
        else
        {
            temp_n[Ii]=1;
        }
            }
	foocompressor(1,count);

	/* Scheduling event and processing them */
    for(Ii=0;Ii<mif_private->conn[4]->size;Ii++)
    {
        if(temp_CompressedCache[Ii]==0)
        {
            _op_CompressedCache[Ii]=ZERO;
            }
        else if(temp_CompressedCache[Ii]==1)
        {
            _op_CompressedCache[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(mif_private->circuit.anal_type == DC)
        {
            ((Digital_t*)(mif_private->conn[4]->port[Ii]->output.pvalue))->state = _op_CompressedCache[Ii];
            }
        else if(_op_CompressedCache[Ii] != _op_CompressedCache_old[Ii])
        {
            ((Digital_t*)(mif_private->conn[4]->port[Ii]->output.pvalue))->state = _op_CompressedCache[Ii];
            mif_private->conn[4]->port[Ii]->delay = ((_op_CompressedCache[Ii] == ZERO) ? mif_private->param[3]->element[0].rvalue : mif_private->param[2]->element[0].rvalue);
            }
        else
        {
            mif_private->conn[4]->port[Ii]->changed = FALSE;
            }
        ((Digital_t*)(mif_private->conn[4]->port[Ii]->output.pvalue))->strength = STRONG;
    }
	/* Scheduling event and processing them */
    for(Ii=0;Ii<mif_private->conn[5]->size;Ii++)
    {
        if(temp_DeCompressedCache[Ii]==0)
        {
            _op_DeCompressedCache[Ii]=ZERO;
            }
        else if(temp_DeCompressedCache[Ii]==1)
        {
            _op_DeCompressedCache[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(mif_private->circuit.anal_type == DC)
        {
            ((Digital_t*)(mif_private->conn[5]->port[Ii]->output.pvalue))->state = _op_DeCompressedCache[Ii];
            }
        else if(_op_DeCompressedCache[Ii] != _op_DeCompressedCache_old[Ii])
        {
            ((Digital_t*)(mif_private->conn[5]->port[Ii]->output.pvalue))->state = _op_DeCompressedCache[Ii];
            mif_private->conn[5]->port[Ii]->delay = ((_op_DeCompressedCache[Ii] == ZERO) ? mif_private->param[3]->element[0].rvalue : mif_private->param[2]->element[0].rvalue);
            }
        else
        {
            mif_private->conn[5]->port[Ii]->changed = FALSE;
            }
        ((Digital_t*)(mif_private->conn[5]->port[Ii]->output.pvalue))->strength = STRONG;
    }

}