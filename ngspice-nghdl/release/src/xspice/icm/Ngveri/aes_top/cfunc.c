#include "ngspice/cm.h"
extern void cm_aes_top(Mif_Private_t *);
/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

                
        #include <stdio.h>
        #include <math.h>
        #include <string.h>
        #include "sim_main_aes_top.h"

        
void cm_aes_top(Mif_Private_t *mif_private) 
{
	Digital_State_t *_op_done, *_op_done_old;
	Digital_State_t *_op_completed_round, *_op_completed_round_old;
	Digital_State_t *_op_cipher_text, *_op_cipher_text_old;

    static int inst_count=0;
    int count=0;
        
    if(mif_private->circuit.init)
    {   
        inst_count++;
        mif_private->param[0]->element[0].rvalue=inst_count;
        fooaes_top(0,inst_count);
        /* Allocate storage for output ports and set the load for input ports */

        
        port_clk=mif_private->conn[0]->size;

        port_start=mif_private->conn[1]->size;

        port_rstn=mif_private->conn[2]->size;

        port_plain_text=mif_private->conn[3]->size;

        port_cipher_key=mif_private->conn[4]->size;

        port_done=mif_private->conn[5]->size;

        port_completed_round=mif_private->conn[6]->size;

        port_cipher_text=mif_private->conn[7]->size;
		cm_event_alloc(0,1*sizeof(Digital_State_t));
		cm_event_alloc(1,10*sizeof(Digital_State_t));
		cm_event_alloc(2,128*sizeof(Digital_State_t));
		/* set the load for input ports. */
		int Ii;
		for(Ii=0;Ii<mif_private->conn[0]->size;Ii++)
		{
			mif_private->conn[0]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}
		for(Ii=0;Ii<mif_private->conn[1]->size;Ii++)
		{
			mif_private->conn[1]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}
		for(Ii=0;Ii<mif_private->conn[2]->size;Ii++)
		{
			mif_private->conn[2]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}
		for(Ii=0;Ii<mif_private->conn[3]->size;Ii++)
		{
			mif_private->conn[3]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}
		for(Ii=0;Ii<mif_private->conn[4]->size;Ii++)
		{
			mif_private->conn[4]->port[Ii]->load=mif_private->param[1]->element[0].rvalue; 
		}

		/*Retrieve Storage for output*/
		_op_done = _op_done_old = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_completed_round = _op_completed_round_old = (Digital_State_t *) cm_event_get_ptr(1,0);
		_op_cipher_text = _op_cipher_text_old = (Digital_State_t *) cm_event_get_ptr(2,0);


	}
	else
	{
		_op_done = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_done_old = (Digital_State_t *) cm_event_get_ptr(0,1);
		_op_completed_round = (Digital_State_t *) cm_event_get_ptr(1,1);
		_op_completed_round_old = (Digital_State_t *) cm_event_get_ptr(1,2);
		_op_cipher_text = (Digital_State_t *) cm_event_get_ptr(2,2);
		_op_cipher_text_old = (Digital_State_t *) cm_event_get_ptr(2,3);
	}

	//Formating data for sending it to client
	int Ii;
	count=(int)mif_private->param[0]->element[0].rvalue;

    for(Ii=0;Ii<mif_private->conn[0]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[0]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_clk[Ii]=0;            }
        else
        {
            temp_clk[Ii]=1;
        }
            }
    for(Ii=0;Ii<mif_private->conn[1]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[1]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_start[Ii]=0;            }
        else
        {
            temp_start[Ii]=1;
        }
            }
    for(Ii=0;Ii<mif_private->conn[2]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[2]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_rstn[Ii]=0;            }
        else
        {
            temp_rstn[Ii]=1;
        }
            }
    for(Ii=0;Ii<mif_private->conn[3]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[3]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_plain_text[Ii]=0;            }
        else
        {
            temp_plain_text[Ii]=1;
        }
            }
    for(Ii=0;Ii<mif_private->conn[4]->size;Ii++)
    {
        if( ((Digital_t*)(mif_private->conn[4]->port[Ii]->input.pvalue))->state==ZERO )
        {
            temp_cipher_key[Ii]=0;            }
        else
        {
            temp_cipher_key[Ii]=1;
        }
            }
	fooaes_top(1,count);

	/* Scheduling event and processing them */
    for(Ii=0;Ii<mif_private->conn[5]->size;Ii++)
    {
        if(temp_done[Ii]==0)
        {
            _op_done[Ii]=ZERO;
            }
        else if(temp_done[Ii]==1)
        {
            _op_done[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(mif_private->circuit.anal_type == DC)
        {
            ((Digital_t*)(mif_private->conn[5]->port[Ii]->output.pvalue))->state = _op_done[Ii];
            }
        else if(_op_done[Ii] != _op_done_old[Ii])
        {
            ((Digital_t*)(mif_private->conn[5]->port[Ii]->output.pvalue))->state = _op_done[Ii];
            mif_private->conn[5]->port[Ii]->delay = ((_op_done[Ii] == ZERO) ? mif_private->param[3]->element[0].rvalue : mif_private->param[2]->element[0].rvalue);
            }
        else
        {
            mif_private->conn[5]->port[Ii]->changed = FALSE;
            }
        ((Digital_t*)(mif_private->conn[5]->port[Ii]->output.pvalue))->strength = STRONG;
    }
	/* Scheduling event and processing them */
    for(Ii=0;Ii<mif_private->conn[6]->size;Ii++)
    {
        if(temp_completed_round[Ii]==0)
        {
            _op_completed_round[Ii]=ZERO;
            }
        else if(temp_completed_round[Ii]==1)
        {
            _op_completed_round[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(mif_private->circuit.anal_type == DC)
        {
            ((Digital_t*)(mif_private->conn[6]->port[Ii]->output.pvalue))->state = _op_completed_round[Ii];
            }
        else if(_op_completed_round[Ii] != _op_completed_round_old[Ii])
        {
            ((Digital_t*)(mif_private->conn[6]->port[Ii]->output.pvalue))->state = _op_completed_round[Ii];
            mif_private->conn[6]->port[Ii]->delay = ((_op_completed_round[Ii] == ZERO) ? mif_private->param[3]->element[0].rvalue : mif_private->param[2]->element[0].rvalue);
            }
        else
        {
            mif_private->conn[6]->port[Ii]->changed = FALSE;
            }
        ((Digital_t*)(mif_private->conn[6]->port[Ii]->output.pvalue))->strength = STRONG;
    }
	/* Scheduling event and processing them */
    for(Ii=0;Ii<mif_private->conn[7]->size;Ii++)
    {
        if(temp_cipher_text[Ii]==0)
        {
            _op_cipher_text[Ii]=ZERO;
            }
        else if(temp_cipher_text[Ii]==1)
        {
            _op_cipher_text[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(mif_private->circuit.anal_type == DC)
        {
            ((Digital_t*)(mif_private->conn[7]->port[Ii]->output.pvalue))->state = _op_cipher_text[Ii];
            }
        else if(_op_cipher_text[Ii] != _op_cipher_text_old[Ii])
        {
            ((Digital_t*)(mif_private->conn[7]->port[Ii]->output.pvalue))->state = _op_cipher_text[Ii];
            mif_private->conn[7]->port[Ii]->delay = ((_op_cipher_text[Ii] == ZERO) ? mif_private->param[3]->element[0].rvalue : mif_private->param[2]->element[0].rvalue);
            }
        else
        {
            mif_private->conn[7]->port[Ii]->changed = FALSE;
            }
        ((Digital_t*)(mif_private->conn[7]->port[Ii]->output.pvalue))->strength = STRONG;
    }

}